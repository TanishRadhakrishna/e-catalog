# E2E Testing

End-to-end tests will be placed in this folder. Recommended tool: Playwright.

To set up Playwright, run:

```
npx playwright install
```

Then create your first test in this folder, e.g. `example.spec.js`.
